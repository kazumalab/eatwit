.top-header
  %ul.menu
    %li
      = link_to("Eatwit", root_path)
    %li
      - if user_signed_in?
        = link_to(current_user.name, user_path(current_user))
      - else
        = link_to("ログイン", new_user_path)
